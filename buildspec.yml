version: 0.1

phases:
  build:
    commands:
      - pwd && ls
      - ./build.sh
      - ls
      - echo ${CODEBUILD_BUILD_ID}
      - export BUILDNUM=$(echo ${CODEBUILD_BUILD_ID} | sed 's/.*://g')
      - echo ${BUILDNUM}

artifacts:
  files:
    - file
